<section class="nav-background">
  <nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
      <a class="navbar-brand text-white" href="/">MovieTime</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
        <ul class="navbar-nav mb-2 mb-lg-0">
          <li class="nav-item dropdown">
            <% if (!userStatus.loggedIn) { %>
              <li class="nav-item "><a href="/login" class="nav-link text-white">Login</a></li>
              <li class="nav-item"><a href="/signup" class="nav-link text-white">Sign Up</a></li>
            <% } else { %>
              <a class="nav-link dropdown-toggle text-white mr-2" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                <!-- <img src="<%= user.profilePic%>" alt="user profile picture" loading="lazy" class="img-fluid rounded nav-profilePic"> -->
                <%= user.userName %>
              </a>
              <ul class="dropdown-menu">
                <li class="dropdown-item"><a href="/<%= user.userName %>" class="nav-link text-white">Profile</a></li>
                <li><hr class="dropdown-divider"></li>
                <li class="dropdown-item"><a href="/settings" class="nav-link text-white">Settings</a></li>
                <li class="dropdown-item"><a href="/logout" class="nav-link text-white">Sign Out</a></li>
              </ul>
            <% } %>
          </li>
        </ul>
        <form action="/movies/search" method="GET" class="d-flex">
          <input class="form-control me-2" name = "query" type="search" placeholder="Search" aria-label="Search">
        </form>
      </div>
    </div>
  </nav>
</section>
